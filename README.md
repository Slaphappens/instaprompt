# InstaPrompt

🚀 Caption-generator med GPT-4 for sosiale medier.

## Mapper

- `mvp/` – Flask-løsning klar til lansering via Railway
- `scalable_version/` – FastAPI + Redis + SendGrid for fremtidig vekst